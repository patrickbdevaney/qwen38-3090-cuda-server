{
 "code_impl": "Implement a thread-safe LRU cache in Python with `get(key)` and `put(key, value)` in O(1). Use a dict plus a doubly linked list, guard every public method with a single reentrant lock, and evict the least recently used entry when `capacity` is exceeded. Include type hints and a short docstring on the class. Write only the code.\n\n```python\nimport threading\nfrom typing import Optional, Dict\n\nclass LRUCache:\n",
 "code_debug": "The function below is supposed to return the k largest elements of `nums` in descending order, but it returns them ascending and drops duplicates. Explain both bugs and give the corrected function.\n\n```python\ndef top_k(nums, k):\n    seen = set(nums)\n    return sorted(seen)[:k]\n```\n\nThe bugs are:\n",
 "tool_call": "You have these tools:\n  read_file(path: str) -> str\n  list_dir(path: str) -> list[str]\n  write_file(path: str, content: str) -> None\n  run_tests(pattern: str) -> str\n\nTask: the test `tests/test_parser.py::test_nested` fails. Find the parser source, read it, fix the nesting bug, and re-run just that test. Emit one tool call per line as strict JSON with keys \"tool\" and \"args\", no prose.\n\n{\"tool\": \"list_dir\", \"args\": {\"path\": \"tests\"}}\n",
 "json_struct": "Convert this changelog into strict JSON matching the schema {\"version\": str, \"date\": str, \"changes\": [{\"kind\": \"added\"|\"fixed\"|\"removed\", \"text\": str}]}. Emit only JSON.\n\nv2.4.0 - 2024-11-03\n * added streaming responses to the /chat endpoint\n * fixed a crash when the prompt exceeded the context window\n * removed the deprecated --legacy-tokenizer flag\n * fixed incorrect token counts in the usage field\n\n{\n",
 "world_knowledge": "Answer each briefly and factually.\n1. Which memory technology do NVIDIA's RTX 3090 and 3090 Ti use, and what is the 3090's peak theoretical bandwidth?\n2. What does the CUDA compute capability sm_86 correspond to?\n3. In the Transformer paper, what is the purpose of scaling the dot product by 1/sqrt(d_k)?\n4. What distinguishes a gated delta network from standard softmax attention in terms of memory growth with sequence length?\n\n1.",
 "math_reason": "A batch job processes 3 shards in parallel. Shard A takes 14 minutes, shard B takes 9 minutes, and shard C takes 21 minutes. After all shards finish there is a 6-minute merge step. The job is retried from scratch if the merge fails, which happens 25% of the time, and a retry never fails. What is the expected total wall-clock time? Show the reasoning step by step, then give the number.\n\nStep 1:",
 "multihop_short": "# Repository map\n## module_0: subsystem_0\nOwns `Subsystem0` and depends on module_0. Exposes `run_0(cfg)` and `reset_0()`. Config key `subsystem_0.enabled` defaults to false.\n## module_1: subsystem_1\nOwns `Subsystem1` and depends on module_0. Exposes `run_1(cfg)` and `reset_1()`. Config key `subsystem_1.enabled` defaults to false.\n## module_2: subsystem_2\nOwns `Subsystem2` and depends on module_1. Exposes `run_2(cfg)` and `reset_2()`. Config key `subsystem_2.enabled` defaults to false.\n## module_3: subsystem_3\nOwns `Subsystem3` and depends on module_2. Exposes `run_3(cfg)` and `reset_3()`. Config key `subsystem_3.enabled` defaults to false.\n## module_4: subsystem_4\nOwns `Subsystem4` and depends on module_3. Exposes `run_4(cfg)` and `reset_4()`. Config key `subsystem_4.enabled` defaults to false.\n## module_5: subsystem_5\nOwns `Subsystem5` and depends on module_4. Exposes `run_5(cfg)` and `reset_5()`. Config key `subsystem_5.enabled` defaults to false.\n## module_6: subsystem_6\nOwns `Subsystem6` and depends on module_5. Exposes `run_6(cfg)` and `reset_6()`. Config key `subsystem_6.enabled` defaults to false.\n## module_7: subsystem_7\nOwns `Subsystem7` and depends on module_6. Exposes `run_7(cfg)` and `reset_7()`. Config key `subsystem_7.enabled` defaults to false.\n## module_8: subsystem_8\nOwns `Subsystem8` and depends on module_7. Exposes `run_8(cfg)` and `reset_8()`. Config key `subsystem_8.enabled` defaults to false.\n## module_9: auth\nHandles session tokens. The signing key is rotated by `rotate_key()` every 900 seconds; nothing else in the tree may call it.\n## module_10: subsystem_10\nOwns `Subsystem10` and depends on module_9. Exposes `run_10(cfg)` and `reset_10()`. Config key `subsystem_10.enabled` defaults to false.\n## module_11: subsystem_11\nOwns `Subsystem11` and depends on module_10. Exposes `run_11(cfg)` and `reset_11()`. Config key `subsystem_11.enabled` defaults to false.\n## module_12: subsystem_12\nOwns `Subsystem12` and depends on module_11. Exposes `run_12(cfg)` and `reset_12()`. Config key `subsystem_12.enabled` defaults to false.\n## module_13: subsystem_13\nOwns `Subsystem13` and depends on module_12. Exposes `run_13(cfg)` and `reset_13()`. Config key `subsystem_13.enabled` defaults to false.\n## module_14: subsystem_14\nOwns `Subsystem14` and depends on module_13. Exposes `run_14(cfg)` and `reset_14()`. Config key `subsystem_14.enabled` defaults to false.\n## module_15: subsystem_15\nOwns `Subsystem15` and depends on module_14. Exposes `run_15(cfg)` and `reset_15()`. Config key `subsystem_15.enabled` defaults to false.\n## module_16: subsystem_16\nOwns `Subsystem16` and depends on module_15. Exposes `run_16(cfg)` and `reset_16()`. Config key `subsystem_16.enabled` defaults to false.\n## module_17: subsystem_17\nOwns `Subsystem17` and depends on module_16. Exposes `run_17(cfg)` and `reset_17()`. Config key `subsystem_17.enabled` defaults to false.\n## module_18: subsystem_18\nOwns `Subsystem18` and depends on module_17. Exposes `run_18(cfg)` and `reset_18()`. Config key `subsystem_18.enabled` defaults to false.\n## module_19: subsystem_19\nOwns `Subsystem19` and depends on module_18. Exposes `run_19(cfg)` and `reset_19()`. Config key `subsystem_19.enabled` defaults to false.\n## module_20: subsystem_20\nOwns `Subsystem20` and depends on module_19. Exposes `run_20(cfg)` and `reset_20()`. Config key `subsystem_20.enabled` defaults to false.\n## module_21: subsystem_21\nOwns `Subsystem21` and depends on module_20. Exposes `run_21(cfg)` and `reset_21()`. Config key `subsystem_21.enabled` defaults to false.\n## module_22: subsystem_22\nOwns `Subsystem22` and depends on module_21. Exposes `run_22(cfg)` and `reset_22()`. Config key `subsystem_22.enabled` defaults to false.\n## module_23: subsystem_23\nOwns `Subsystem23` and depends on module_22. Exposes `run_23(cfg)` and `reset_23()`. Config key `subsystem_23.enabled` defaults to false.\n\n# Question\nIn the repository map above, how often is the auth signing key rotated, and which function performs the rotation? Answer in one sentence, citing the module.\n\n# Answer\n",
 "multihop_long": "# Repository map\n## module_0: subsystem_0\nOwns `Subsystem0` and depends on module_0. Exposes `run_0(cfg)` and `reset_0()`. Config key `subsystem_0.enabled` defaults to false.\n## module_1: subsystem_1\nOwns `Subsystem1` and depends on module_0. Exposes `run_1(cfg)` and `reset_1()`. Config key `subsystem_1.enabled` defaults to false.\n## module_2: subsystem_2\nOwns `Subsystem2` and depends on module_1. Exposes `run_2(cfg)` and `reset_2()`. Config key `subsystem_2.enabled` defaults to false.\n## module_3: subsystem_3\nOwns `Subsystem3` and depends on module_2. Exposes `run_3(cfg)` and `reset_3()`. Config key `subsystem_3.enabled` defaults to false.\n## module_4: subsystem_4\nOwns `Subsystem4` and depends on module_3. Exposes `run_4(cfg)` and `reset_4()`. Config key `subsystem_4.enabled` defaults to false.\n## module_5: subsystem_5\nOwns `Subsystem5` and depends on module_4. Exposes `run_5(cfg)` and `reset_5()`. Config key `subsystem_5.enabled` defaults to false.\n## module_6: subsystem_6\nOwns `Subsystem6` and depends on module_5. Exposes `run_6(cfg)` and `reset_6()`. Config key `subsystem_6.enabled` defaults to false.\n## module_7: subsystem_7\nOwns `Subsystem7` and depends on module_6. Exposes `run_7(cfg)` and `reset_7()`. Config key `subsystem_7.enabled` defaults to false.\n## module_8: subsystem_8\nOwns `Subsystem8` and depends on module_7. Exposes `run_8(cfg)` and `reset_8()`. Config key `subsystem_8.enabled` defaults to false.\n## module_9: subsystem_9\nOwns `Subsystem9` and depends on module_8. Exposes `run_9(cfg)` and `reset_9()`. Config key `subsystem_9.enabled` defaults to false.\n## module_10: subsystem_10\nOwns `Subsystem10` and depends on module_9. Exposes `run_10(cfg)` and `reset_10()`. Config key `subsystem_10.enabled` defaults to false.\n## module_11: subsystem_11\nOwns `Subsystem11` and depends on module_10. Exposes `run_11(cfg)` and `reset_11()`. Config key `subsystem_11.enabled` defaults to false.\n## module_12: subsystem_12\nOwns `Subsystem12` and depends on module_11. Exposes `run_12(cfg)` and `reset_12()`. Config key `subsystem_12.enabled` defaults to false.\n## module_13: subsystem_13\nOwns `Subsystem13` and depends on module_12. Exposes `run_13(cfg)` and `reset_13()`. Config key `subsystem_13.enabled` defaults to false.\n## module_14: subsystem_14\nOwns `Subsystem14` and depends on module_13. Exposes `run_14(cfg)` and `reset_14()`. Config key `subsystem_14.enabled` defaults to false.\n## module_15: subsystem_15\nOwns `Subsystem15` and depends on module_14. Exposes `run_15(cfg)` and `reset_15()`. Config key `subsystem_15.enabled` defaults to false.\n## module_16: subsystem_16\nOwns `Subsystem16` and depends on module_15. Exposes `run_16(cfg)` and `reset_16()`. Config key `subsystem_16.enabled` defaults to false.\n## module_17: subsystem_17\nOwns `Subsystem17` and depends on module_16. Exposes `run_17(cfg)` and `reset_17()`. Config key `subsystem_17.enabled` defaults to false.\n## module_18: subsystem_18\nOwns `Subsystem18` and depends on module_17. Exposes `run_18(cfg)` and `reset_18()`. Config key `subsystem_18.enabled` defaults to false.\n## module_19: subsystem_19\nOwns `Subsystem19` and depends on module_18. Exposes `run_19(cfg)` and `reset_19()`. Config key `subsystem_19.enabled` defaults to false.\n## module_20: subsystem_20\nOwns `Subsystem20` and depends on module_19. Exposes `run_20(cfg)` and `reset_20()`. Config key `subsystem_20.enabled` defaults to false.\n## module_21: subsystem_21\nOwns `Subsystem21` and depends on module_20. Exposes `run_21(cfg)` and `reset_21()`. Config key `subsystem_21.enabled` defaults to false.\n## module_22: subsystem_22\nOwns `Subsystem22` and depends on module_21. Exposes `run_22(cfg)` and `reset_22()`. Config key `subsystem_22.enabled` defaults to false.\n## module_23: subsystem_23\nOwns `Subsystem23` and depends on module_22. Exposes `run_23(cfg)` and `reset_23()`. Config key `subsystem_23.enabled` defaults to false.\n## module_24: subsystem_24\nOwns `Subsystem24` and depends on module_23. Exposes `run_24(cfg)` and `reset_24()`. Config key `subsystem_24.enabled` defaults to false.\n## module_25: subsystem_25\nOwns `Subsystem25` and depends on module_24. Exposes `run_25(cfg)` and `reset_25()`. Config key `subsystem_25.enabled` defaults to false.\n## module_26: subsystem_26\nOwns `Subsystem26` and depends on module_25. Exposes `run_26(cfg)` and `reset_26()`. Config key `subsystem_26.enabled` defaults to false.\n## module_27: subsystem_27\nOwns `Subsystem27` and depends on module_26. Exposes `run_27(cfg)` and `reset_27()`. Config key `subsystem_27.enabled` defaults to false.\n## module_28: subsystem_28\nOwns `Subsystem28` and depends on module_27. Exposes `run_28(cfg)` and `reset_28()`. Config key `subsystem_28.enabled` defaults to false.\n## module_29: subsystem_29\nOwns `Subsystem29` and depends on module_28. Exposes `run_29(cfg)` and `reset_29()`. Config key `subsystem_29.enabled` defaults to false.\n## module_30: subsystem_30\nOwns `Subsystem30` and depends on module_29. Exposes `run_30(cfg)` and `reset_30()`. Config key `subsystem_30.enabled` defaults to false.\n## module_31: auth\nHandles session tokens. The signing key is rotated by `rotate_key()` every 1800 seconds; nothing else in the tree may call it.\n## module_32: subsystem_32\nOwns `Subsystem32` and depends on module_31. Exposes `run_32(cfg)` and `reset_32()`. Config key `subsystem_32.enabled` defaults to false.\n## module_33: subsystem_33\nOwns `Subsystem33` and depends on module_32. Exposes `run_33(cfg)` and `reset_33()`. Config key `subsystem_33.enabled` defaults to false.\n## module_34: subsystem_34\nOwns `Subsystem34` and depends on module_33. Exposes `run_34(cfg)` and `reset_34()`. Config key `subsystem_34.enabled` defaults to false.\n## module_35: subsystem_35\nOwns `Subsystem35` and depends on module_34. Exposes `run_35(cfg)` and `reset_35()`. Config key `subsystem_35.enabled` defaults to false.\n## module_36: subsystem_36\nOwns `Subsystem36` and depends on module_35. Exposes `run_36(cfg)` and `reset_36()`. Config key `subsystem_36.enabled` defaults to false.\n## module_37: subsystem_37\nOwns `Subsystem37` and depends on module_36. Exposes `run_37(cfg)` and `reset_37()`. Config key `subsystem_37.enabled` defaults to false.\n## module_38: subsystem_38\nOwns `Subsystem38` and depends on module_37. Exposes `run_38(cfg)` and `reset_38()`. Config key `subsystem_38.enabled` defaults to false.\n## module_39: subsystem_39\nOwns `Subsystem39` and depends on module_38. Exposes `run_39(cfg)` and `reset_39()`. Config key `subsystem_39.enabled` defaults to false.\n## module_40: subsystem_40\nOwns `Subsystem40` and depends on module_39. Exposes `run_40(cfg)` and `reset_40()`. Config key `subsystem_40.enabled` defaults to false.\n## module_41: subsystem_41\nOwns `Subsystem41` and depends on module_40. Exposes `run_41(cfg)` and `reset_41()`. Config key `subsystem_41.enabled` defaults to false.\n## module_42: subsystem_42\nOwns `Subsystem42` and depends on module_41. Exposes `run_42(cfg)` and `reset_42()`. Config key `subsystem_42.enabled` defaults to false.\n## module_43: subsystem_43\nOwns `Subsystem43` and depends on module_42. Exposes `run_43(cfg)` and `reset_43()`. Config key `subsystem_43.enabled` defaults to false.\n## module_44: subsystem_44\nOwns `Subsystem44` and depends on module_43. Exposes `run_44(cfg)` and `reset_44()`. Config key `subsystem_44.enabled` defaults to false.\n## module_45: subsystem_45\nOwns `Subsystem45` and depends on module_44. Exposes `run_45(cfg)` and `reset_45()`. Config key `subsystem_45.enabled` defaults to false.\n## module_46: subsystem_46\nOwns `Subsystem46` and depends on module_45. Exposes `run_46(cfg)` and `reset_46()`. Config key `subsystem_46.enabled` defaults to false.\n## module_47: subsystem_47\nOwns `Subsystem47` and depends on module_46. Exposes `run_47(cfg)` and `reset_47()`. Config key `subsystem_47.enabled` defaults to false.\n## module_48: subsystem_48\nOwns `Subsystem48` and depends on module_47. Exposes `run_48(cfg)` and `reset_48()`. Config key `subsystem_48.enabled` defaults to false.\n## module_49: subsystem_49\nOwns `Subsystem49` and depends on module_48. Exposes `run_49(cfg)` and `reset_49()`. Config key `subsystem_49.enabled` defaults to false.\n## module_50: subsystem_50\nOwns `Subsystem50` and depends on module_49. Exposes `run_50(cfg)` and `reset_50()`. Config key `subsystem_50.enabled` defaults to false.\n## module_51: subsystem_51\nOwns `Subsystem51` and depends on module_50. Exposes `run_51(cfg)` and `reset_51()`. Config key `subsystem_51.enabled` defaults to false.\n## module_52: subsystem_52\nOwns `Subsystem52` and depends on module_51. Exposes `run_52(cfg)` and `reset_52()`. Config key `subsystem_52.enabled` defaults to false.\n## module_53: subsystem_53\nOwns `Subsystem53` and depends on module_52. Exposes `run_53(cfg)` and `reset_53()`. Config key `subsystem_53.enabled` defaults to false.\n## module_54: subsystem_54\nOwns `Subsystem54` and depends on module_53. Exposes `run_54(cfg)` and `reset_54()`. Config key `subsystem_54.enabled` defaults to false.\n## module_55: subsystem_55\nOwns `Subsystem55` and depends on module_54. Exposes `run_55(cfg)` and `reset_55()`. Config key `subsystem_55.enabled` defaults to false.\n## module_56: subsystem_56\nOwns `Subsystem56` and depends on module_55. Exposes `run_56(cfg)` and `reset_56()`. Config key `subsystem_56.enabled` defaults to false.\n## module_57: subsystem_57\nOwns `Subsystem57` and depends on module_56. Exposes `run_57(cfg)` and `reset_57()`. Config key `subsystem_57.enabled` defaults to false.\n## module_58: subsystem_58\nOwns `Subsystem58` and depends on module_57. Exposes `run_58(cfg)` and `reset_58()`. Config key `subsystem_58.enabled` defaults to false.\n## module_59: subsystem_59\nOwns `Subsystem59` and depends on module_58. Exposes `run_59(cfg)` and `reset_59()`. Config key `subsystem_59.enabled` defaults to false.\n## module_60: subsystem_60\nOwns `Subsystem60` and depends on module_59. Exposes `run_60(cfg)` and `reset_60()`. Config key `subsystem_60.enabled` defaults to false.\n## module_61: subsystem_61\nOwns `Subsystem61` and depends on module_60. Exposes `run_61(cfg)` and `reset_61()`. Config key `subsystem_61.enabled` defaults to false.\n## module_62: subsystem_62\nOwns `Subsystem62` and depends on module_61. Exposes `run_62(cfg)` and `reset_62()`. Config key `subsystem_62.enabled` defaults to false.\n## module_63: subsystem_63\nOwns `Subsystem63` and depends on module_62. Exposes `run_63(cfg)` and `reset_63()`. Config key `subsystem_63.enabled` defaults to false.\n## module_64: subsystem_64\nOwns `Subsystem64` and depends on module_63. Exposes `run_64(cfg)` and `reset_64()`. Config key `subsystem_64.enabled` defaults to false.\n## module_65: subsystem_65\nOwns `Subsystem65` and depends on module_64. Exposes `run_65(cfg)` and `reset_65()`. Config key `subsystem_65.enabled` defaults to false.\n## module_66: subsystem_66\nOwns `Subsystem66` and depends on module_65. Exposes `run_66(cfg)` and `reset_66()`. Config key `subsystem_66.enabled` defaults to false.\n## module_67: subsystem_67\nOwns `Subsystem67` and depends on module_66. Exposes `run_67(cfg)` and `reset_67()`. Config key `subsystem_67.enabled` defaults to false.\n## module_68: subsystem_68\nOwns `Subsystem68` and depends on module_67. Exposes `run_68(cfg)` and `reset_68()`. Config key `subsystem_68.enabled` defaults to false.\n## module_69: subsystem_69\nOwns `Subsystem69` and depends on module_68. Exposes `run_69(cfg)` and `reset_69()`. Config key `subsystem_69.enabled` defaults to false.\n## module_70: subsystem_70\nOwns `Subsystem70` and depends on module_69. Exposes `run_70(cfg)` and `reset_70()`. Config key `subsystem_70.enabled` defaults to false.\n## module_71: subsystem_71\nOwns `Subsystem71` and depends on module_70. Exposes `run_71(cfg)` and `reset_71()`. Config key `subsystem_71.enabled` defaults to false.\n## module_72: subsystem_72\nOwns `Subsystem72` and depends on module_71. Exposes `run_72(cfg)` and `reset_72()`. Config key `subsystem_72.enabled` defaults to false.\n## module_73: subsystem_73\nOwns `Subsystem73` and depends on module_72. Exposes `run_73(cfg)` and `reset_73()`. Config key `subsystem_73.enabled` defaults to false.\n## module_74: subsystem_74\nOwns `Subsystem74` and depends on module_73. Exposes `run_74(cfg)` and `reset_74()`. Config key `subsystem_74.enabled` defaults to false.\n## module_75: subsystem_75\nOwns `Subsystem75` and depends on module_74. Exposes `run_75(cfg)` and `reset_75()`. Config key `subsystem_75.enabled` defaults to false.\n## module_76: subsystem_76\nOwns `Subsystem76` and depends on module_75. Exposes `run_76(cfg)` and `reset_76()`. Config key `subsystem_76.enabled` defaults to false.\n## module_77: subsystem_77\nOwns `Subsystem77` and depends on module_76. Exposes `run_77(cfg)` and `reset_77()`. Config key `subsystem_77.enabled` defaults to false.\n## module_78: subsystem_78\nOwns `Subsystem78` and depends on module_77. Exposes `run_78(cfg)` and `reset_78()`. Config key `subsystem_78.enabled` defaults to false.\n## module_79: subsystem_79\nOwns `Subsystem79` and depends on module_78. Exposes `run_79(cfg)` and `reset_79()`. Config key `subsystem_79.enabled` defaults to false.\n## module_80: subsystem_80\nOwns `Subsystem80` and depends on module_79. Exposes `run_80(cfg)` and `reset_80()`. Config key `subsystem_80.enabled` defaults to false.\n## module_81: subsystem_81\nOwns `Subsystem81` and depends on module_80. Exposes `run_81(cfg)` and `reset_81()`. Config key `subsystem_81.enabled` defaults to false.\n## module_82: subsystem_82\nOwns `Subsystem82` and depends on module_81. Exposes `run_82(cfg)` and `reset_82()`. Config key `subsystem_82.enabled` defaults to false.\n## module_83: subsystem_83\nOwns `Subsystem83` and depends on module_82. Exposes `run_83(cfg)` and `reset_83()`. Config key `subsystem_83.enabled` defaults to false.\n## module_84: subsystem_84\nOwns `Subsystem84` and depends on module_83. Exposes `run_84(cfg)` and `reset_84()`. Config key `subsystem_84.enabled` defaults to false.\n## module_85: subsystem_85\nOwns `Subsystem85` and depends on module_84. Exposes `run_85(cfg)` and `reset_85()`. Config key `subsystem_85.enabled` defaults to false.\n## module_86: subsystem_86\nOwns `Subsystem86` and depends on module_85. Exposes `run_86(cfg)` and `reset_86()`. Config key `subsystem_86.enabled` defaults to false.\n## module_87: subsystem_87\nOwns `Subsystem87` and depends on module_86. Exposes `run_87(cfg)` and `reset_87()`. Config key `subsystem_87.enabled` defaults to false.\n## module_88: subsystem_88\nOwns `Subsystem88` and depends on module_87. Exposes `run_88(cfg)` and `reset_88()`. Config key `subsystem_88.enabled` defaults to false.\n## module_89: subsystem_89\nOwns `Subsystem89` and depends on module_88. Exposes `run_89(cfg)` and `reset_89()`. Config key `subsystem_89.enabled` defaults to false.\n## module_90: subsystem_90\nOwns `Subsystem90` and depends on module_89. Exposes `run_90(cfg)` and `reset_90()`. Config key `subsystem_90.enabled` defaults to false.\n## module_91: subsystem_91\nOwns `Subsystem91` and depends on module_90. Exposes `run_91(cfg)` and `reset_91()`. Config key `subsystem_91.enabled` defaults to false.\n## module_92: subsystem_92\nOwns `Subsystem92` and depends on module_91. Exposes `run_92(cfg)` and `reset_92()`. Config key `subsystem_92.enabled` defaults to false.\n## module_93: subsystem_93\nOwns `Subsystem93` and depends on module_92. Exposes `run_93(cfg)` and `reset_93()`. Config key `subsystem_93.enabled` defaults to false.\n## module_94: subsystem_94\nOwns `Subsystem94` and depends on module_93. Exposes `run_94(cfg)` and `reset_94()`. Config key `subsystem_94.enabled` defaults to false.\n## module_95: subsystem_95\nOwns `Subsystem95` and depends on module_94. Exposes `run_95(cfg)` and `reset_95()`. Config key `subsystem_95.enabled` defaults to false.\n## module_96: subsystem_96\nOwns `Subsystem96` and depends on module_95. Exposes `run_96(cfg)` and `reset_96()`. Config key `subsystem_96.enabled` defaults to false.\n## module_97: subsystem_97\nOwns `Subsystem97` and depends on module_96. Exposes `run_97(cfg)` and `reset_97()`. Config key `subsystem_97.enabled` defaults to false.\n## module_98: subsystem_98\nOwns `Subsystem98` and depends on module_97. Exposes `run_98(cfg)` and `reset_98()`. Config key `subsystem_98.enabled` defaults to false.\n## module_99: subsystem_99\nOwns `Subsystem99` and depends on module_98. Exposes `run_99(cfg)` and `reset_99()`. Config key `subsystem_99.enabled` defaults to false.\n## module_100: subsystem_100\nOwns `Subsystem100` and depends on module_99. Exposes `run_100(cfg)` and `reset_100()`. Config key `subsystem_100.enabled` defaults to false.\n## module_101: subsystem_101\nOwns `Subsystem101` and depends on module_100. Exposes `run_101(cfg)` and `reset_101()`. Config key `subsystem_101.enabled` defaults to false.\n## module_102: subsystem_102\nOwns `Subsystem102` and depends on module_101. Exposes `run_102(cfg)` and `reset_102()`. Config key `subsystem_102.enabled` defaults to false.\n## module_103: subsystem_103\nOwns `Subsystem103` and depends on module_102. Exposes `run_103(cfg)` and `reset_103()`. Config key `subsystem_103.enabled` defaults to false.\n## module_104: subsystem_104\nOwns `Subsystem104` and depends on module_103. Exposes `run_104(cfg)` and `reset_104()`. Config key `subsystem_104.enabled` defaults to false.\n## module_105: subsystem_105\nOwns `Subsystem105` and depends on module_104. Exposes `run_105(cfg)` and `reset_105()`. Config key `subsystem_105.enabled` defaults to false.\n## module_106: subsystem_106\nOwns `Subsystem106` and depends on module_105. Exposes `run_106(cfg)` and `reset_106()`. Config key `subsystem_106.enabled` defaults to false.\n## module_107: subsystem_107\nOwns `Subsystem107` and depends on module_106. Exposes `run_107(cfg)` and `reset_107()`. Config key `subsystem_107.enabled` defaults to false.\n## module_108: subsystem_108\nOwns `Subsystem108` and depends on module_107. Exposes `run_108(cfg)` and `reset_108()`. Config key `subsystem_108.enabled` defaults to false.\n## module_109: subsystem_109\nOwns `Subsystem109` and depends on module_108. Exposes `run_109(cfg)` and `reset_109()`. Config key `subsystem_109.enabled` defaults to false.\n## module_110: subsystem_110\nOwns `Subsystem110` and depends on module_109. Exposes `run_110(cfg)` and `reset_110()`. Config key `subsystem_110.enabled` defaults to false.\n## module_111: subsystem_111\nOwns `Subsystem111` and depends on module_110. Exposes `run_111(cfg)` and `reset_111()`. Config key `subsystem_111.enabled` defaults to false.\n## module_112: subsystem_112\nOwns `Subsystem112` and depends on module_111. Exposes `run_112(cfg)` and `reset_112()`. Config key `subsystem_112.enabled` defaults to false.\n## module_113: subsystem_113\nOwns `Subsystem113` and depends on module_112. Exposes `run_113(cfg)` and `reset_113()`. Config key `subsystem_113.enabled` defaults to false.\n## module_114: subsystem_114\nOwns `Subsystem114` and depends on module_113. Exposes `run_114(cfg)` and `reset_114()`. Config key `subsystem_114.enabled` defaults to false.\n## module_115: subsystem_115\nOwns `Subsystem115` and depends on module_114. Exposes `run_115(cfg)` and `reset_115()`. Config key `subsystem_115.enabled` defaults to false.\n## module_116: subsystem_116\nOwns `Subsystem116` and depends on module_115. Exposes `run_116(cfg)` and `reset_116()`. Config key `subsystem_116.enabled` defaults to false.\n## module_117: subsystem_117\nOwns `Subsystem117` and depends on module_116. Exposes `run_117(cfg)` and `reset_117()`. Config key `subsystem_117.enabled` defaults to false.\n## module_118: subsystem_118\nOwns `Subsystem118` and depends on module_117. Exposes `run_118(cfg)` and `reset_118()`. Config key `subsystem_118.enabled` defaults to false.\n## module_119: subsystem_119\nOwns `Subsystem119` and depends on module_118. Exposes `run_119(cfg)` and `reset_119()`. Config key `subsystem_119.enabled` defaults to false.\n## module_120: subsystem_120\nOwns `Subsystem120` and depends on module_119. Exposes `run_120(cfg)` and `reset_120()`. Config key `subsystem_120.enabled` defaults to false.\n## module_121: subsystem_121\nOwns `Subsystem121` and depends on module_120. Exposes `run_121(cfg)` and `reset_121()`. Config key `subsystem_121.enabled` defaults to false.\n## module_122: subsystem_122\nOwns `Subsystem122` and depends on module_121. Exposes `run_122(cfg)` and `reset_122()`. Config key `subsystem_122.enabled` defaults to false.\n## module_123: subsystem_123\nOwns `Subsystem123` and depends on module_122. Exposes `run_123(cfg)` and `reset_123()`. Config key `subsystem_123.enabled` defaults to false.\n## module_124: subsystem_124\nOwns `Subsystem124` and depends on module_123. Exposes `run_124(cfg)` and `reset_124()`. Config key `subsystem_124.enabled` defaults to false.\n## module_125: subsystem_125\nOwns `Subsystem125` and depends on module_124. Exposes `run_125(cfg)` and `reset_125()`. Config key `subsystem_125.enabled` defaults to false.\n## module_126: subsystem_126\nOwns `Subsystem126` and depends on module_125. Exposes `run_126(cfg)` and `reset_126()`. Config key `subsystem_126.enabled` defaults to false.\n## module_127: subsystem_127\nOwns `Subsystem127` and depends on module_126. Exposes `run_127(cfg)` and `reset_127()`. Config key `subsystem_127.enabled` defaults to false.\n## module_128: subsystem_128\nOwns `Subsystem128` and depends on module_127. Exposes `run_128(cfg)` and `reset_128()`. Config key `subsystem_128.enabled` defaults to false.\n## module_129: subsystem_129\nOwns `Subsystem129` and depends on module_128. Exposes `run_129(cfg)` and `reset_129()`. Config key `subsystem_129.enabled` defaults to false.\n## module_130: subsystem_130\nOwns `Subsystem130` and depends on module_129. Exposes `run_130(cfg)` and `reset_130()`. Config key `subsystem_130.enabled` defaults to false.\n## module_131: subsystem_131\nOwns `Subsystem131` and depends on module_130. Exposes `run_131(cfg)` and `reset_131()`. Config key `subsystem_131.enabled` defaults to false.\n## module_132: subsystem_132\nOwns `Subsystem132` and depends on module_131. Exposes `run_132(cfg)` and `reset_132()`. Config key `subsystem_132.enabled` defaults to false.\n## module_133: subsystem_133\nOwns `Subsystem133` and depends on module_132. Exposes `run_133(cfg)` and `reset_133()`. Config key `subsystem_133.enabled` defaults to false.\n## module_134: subsystem_134\nOwns `Subsystem134` and depends on module_133. Exposes `run_134(cfg)` and `reset_134()`. Config key `subsystem_134.enabled` defaults to false.\n## module_135: subsystem_135\nOwns `Subsystem135` and depends on module_134. Exposes `run_135(cfg)` and `reset_135()`. Config key `subsystem_135.enabled` defaults to false.\n## module_136: subsystem_136\nOwns `Subsystem136` and depends on module_135. Exposes `run_136(cfg)` and `reset_136()`. Config key `subsystem_136.enabled` defaults to false.\n## module_137: subsystem_137\nOwns `Subsystem137` and depends on module_136. Exposes `run_137(cfg)` and `reset_137()`. Config key `subsystem_137.enabled` defaults to false.\n## module_138: subsystem_138\nOwns `Subsystem138` and depends on module_137. Exposes `run_138(cfg)` and `reset_138()`. Config key `subsystem_138.enabled` defaults to false.\n## module_139: subsystem_139\nOwns `Subsystem139` and depends on module_138. Exposes `run_139(cfg)` and `reset_139()`. Config key `subsystem_139.enabled` defaults to false.\n## module_140: subsystem_140\nOwns `Subsystem140` and depends on module_139. Exposes `run_140(cfg)` and `reset_140()`. Config key `subsystem_140.enabled` defaults to false.\n## module_141: subsystem_141\nOwns `Subsystem141` and depends on module_140. Exposes `run_141(cfg)` and `reset_141()`. Config key `subsystem_141.enabled` defaults to false.\n## module_142: subsystem_142\nOwns `Subsystem142` and depends on module_141. Exposes `run_142(cfg)` and `reset_142()`. Config key `subsystem_142.enabled` defaults to false.\n## module_143: subsystem_143\nOwns `Subsystem143` and depends on module_142. Exposes `run_143(cfg)` and `reset_143()`. Config key `subsystem_143.enabled` defaults to false.\n## module_144: subsystem_144\nOwns `Subsystem144` and depends on module_143. Exposes `run_144(cfg)` and `reset_144()`. Config key `subsystem_144.enabled` defaults to false.\n## module_145: subsystem_145\nOwns `Subsystem145` and depends on module_144. Exposes `run_145(cfg)` and `reset_145()`. Config key `subsystem_145.enabled` defaults to false.\n## module_146: subsystem_146\nOwns `Subsystem146` and depends on module_145. Exposes `run_146(cfg)` and `reset_146()`. Config key `subsystem_146.enabled` defaults to false.\n## module_147: subsystem_147\nOwns `Subsystem147` and depends on module_146. Exposes `run_147(cfg)` and `reset_147()`. Config key `subsystem_147.enabled` defaults to false.\n## module_148: subsystem_148\nOwns `Subsystem148` and depends on module_147. Exposes `run_148(cfg)` and `reset_148()`. Config key `subsystem_148.enabled` defaults to false.\n## module_149: subsystem_149\nOwns `Subsystem149` and depends on module_148. Exposes `run_149(cfg)` and `reset_149()`. Config key `subsystem_149.enabled` defaults to false.\n## module_150: subsystem_150\nOwns `Subsystem150` and depends on module_149. Exposes `run_150(cfg)` and `reset_150()`. Config key `subsystem_150.enabled` defaults to false.\n## module_151: subsystem_151\nOwns `Subsystem151` and depends on module_150. Exposes `run_151(cfg)` and `reset_151()`. Config key `subsystem_151.enabled` defaults to false.\n## module_152: subsystem_152\nOwns `Subsystem152` and depends on module_151. Exposes `run_152(cfg)` and `reset_152()`. Config key `subsystem_152.enabled` defaults to false.\n## module_153: subsystem_153\nOwns `Subsystem153` and depends on module_152. Exposes `run_153(cfg)` and `reset_153()`. Config key `subsystem_153.enabled` defaults to false.\n## module_154: subsystem_154\nOwns `Subsystem154` and depends on module_153. Exposes `run_154(cfg)` and `reset_154()`. Config key `subsystem_154.enabled` defaults to false.\n## module_155: subsystem_155\nOwns `Subsystem155` and depends on module_154. Exposes `run_155(cfg)` and `reset_155()`. Config key `subsystem_155.enabled` defaults to false.\n## module_156: subsystem_156\nOwns `Subsystem156` and depends on module_155. Exposes `run_156(cfg)` and `reset_156()`. Config key `subsystem_156.enabled` defaults to false.\n## module_157: subsystem_157\nOwns `Subsystem157` and depends on module_156. Exposes `run_157(cfg)` and `reset_157()`. Config key `subsystem_157.enabled` defaults to false.\n## module_158: subsystem_158\nOwns `Subsystem158` and depends on module_157. Exposes `run_158(cfg)` and `reset_158()`. Config key `subsystem_158.enabled` defaults to false.\n## module_159: subsystem_159\nOwns `Subsystem159` and depends on module_158. Exposes `run_159(cfg)` and `reset_159()`. Config key `subsystem_159.enabled` defaults to false.\n## module_160: subsystem_160\nOwns `Subsystem160` and depends on module_159. Exposes `run_160(cfg)` and `reset_160()`. Config key `subsystem_160.enabled` defaults to false.\n## module_161: subsystem_161\nOwns `Subsystem161` and depends on module_160. Exposes `run_161(cfg)` and `reset_161()`. Config key `subsystem_161.enabled` defaults to false.\n## module_162: subsystem_162\nOwns `Subsystem162` and depends on module_161. Exposes `run_162(cfg)` and `reset_162()`. Config key `subsystem_162.enabled` defaults to false.\n## module_163: subsystem_163\nOwns `Subsystem163` and depends on module_162. Exposes `run_163(cfg)` and `reset_163()`. Config key `subsystem_163.enabled` defaults to false.\n## module_164: subsystem_164\nOwns `Subsystem164` and depends on module_163. Exposes `run_164(cfg)` and `reset_164()`. Config key `subsystem_164.enabled` defaults to false.\n## module_165: subsystem_165\nOwns `Subsystem165` and depends on module_164. Exposes `run_165(cfg)` and `reset_165()`. Config key `subsystem_165.enabled` defaults to false.\n## module_166: subsystem_166\nOwns `Subsystem166` and depends on module_165. Exposes `run_166(cfg)` and `reset_166()`. Config key `subsystem_166.enabled` defaults to false.\n## module_167: subsystem_167\nOwns `Subsystem167` and depends on module_166. Exposes `run_167(cfg)` and `reset_167()`. Config key `subsystem_167.enabled` defaults to false.\n## module_168: subsystem_168\nOwns `Subsystem168` and depends on module_167. Exposes `run_168(cfg)` and `reset_168()`. Config key `subsystem_168.enabled` defaults to false.\n## module_169: subsystem_169\nOwns `Subsystem169` and depends on module_168. Exposes `run_169(cfg)` and `reset_169()`. Config key `subsystem_169.enabled` defaults to false.\n## module_170: subsystem_170\nOwns `Subsystem170` and depends on module_169. Exposes `run_170(cfg)` and `reset_170()`. Config key `subsystem_170.enabled` defaults to false.\n## module_171: subsystem_171\nOwns `Subsystem171` and depends on module_170. Exposes `run_171(cfg)` and `reset_171()`. Config key `subsystem_171.enabled` defaults to false.\n## module_172: subsystem_172\nOwns `Subsystem172` and depends on module_171. Exposes `run_172(cfg)` and `reset_172()`. Config key `subsystem_172.enabled` defaults to false.\n## module_173: subsystem_173\nOwns `Subsystem173` and depends on module_172. Exposes `run_173(cfg)` and `reset_173()`. Config key `subsystem_173.enabled` defaults to false.\n## module_174: subsystem_174\nOwns `Subsystem174` and depends on module_173. Exposes `run_174(cfg)` and `reset_174()`. Config key `subsystem_174.enabled` defaults to false.\n## module_175: subsystem_175\nOwns `Subsystem175` and depends on module_174. Exposes `run_175(cfg)` and `reset_175()`. Config key `subsystem_175.enabled` defaults to false.\n## module_176: subsystem_176\nOwns `Subsystem176` and depends on module_175. Exposes `run_176(cfg)` and `reset_176()`. Config key `subsystem_176.enabled` defaults to false.\n## module_177: subsystem_177\nOwns `Subsystem177` and depends on module_176. Exposes `run_177(cfg)` and `reset_177()`. Config key `subsystem_177.enabled` defaults to false.\n## module_178: subsystem_178\nOwns `Subsystem178` and depends on module_177. Exposes `run_178(cfg)` and `reset_178()`. Config key `subsystem_178.enabled` defaults to false.\n## module_179: subsystem_179\nOwns `Subsystem179` and depends on module_178. Exposes `run_179(cfg)` and `reset_179()`. Config key `subsystem_179.enabled` defaults to false.\n## module_180: subsystem_180\nOwns `Subsystem180` and depends on module_179. Exposes `run_180(cfg)` and `reset_180()`. Config key `subsystem_180.enabled` defaults to false.\n## module_181: subsystem_181\nOwns `Subsystem181` and depends on module_180. Exposes `run_181(cfg)` and `reset_181()`. Config key `subsystem_181.enabled` defaults to false.\n## module_182: subsystem_182\nOwns `Subsystem182` and depends on module_181. Exposes `run_182(cfg)` and `reset_182()`. Config key `subsystem_182.enabled` defaults to false.\n## module_183: subsystem_183\nOwns `Subsystem183` and depends on module_182. Exposes `run_183(cfg)` and `reset_183()`. Config key `subsystem_183.enabled` defaults to false.\n## module_184: subsystem_184\nOwns `Subsystem184` and depends on module_183. Exposes `run_184(cfg)` and `reset_184()`. Config key `subsystem_184.enabled` defaults to false.\n## module_185: subsystem_185\nOwns `Subsystem185` and depends on module_184. Exposes `run_185(cfg)` and `reset_185()`. Config key `subsystem_185.enabled` defaults to false.\n## module_186: subsystem_186\nOwns `Subsystem186` and depends on module_185. Exposes `run_186(cfg)` and `reset_186()`. Config key `subsystem_186.enabled` defaults to false.\n## module_187: subsystem_187\nOwns `Subsystem187` and depends on module_186. Exposes `run_187(cfg)` and `reset_187()`. Config key `subsystem_187.enabled` defaults to false.\n## module_188: subsystem_188\nOwns `Subsystem188` and depends on module_187. Exposes `run_188(cfg)` and `reset_188()`. Config key `subsystem_188.enabled` defaults to false.\n## module_189: subsystem_189\nOwns `Subsystem189` and depends on module_188. Exposes `run_189(cfg)` and `reset_189()`. Config key `subsystem_189.enabled` defaults to false.\n## module_190: subsystem_190\nOwns `Subsystem190` and depends on module_189. Exposes `run_190(cfg)` and `reset_190()`. Config key `subsystem_190.enabled` defaults to false.\n## module_191: subsystem_191\nOwns `Subsystem191` and depends on module_190. Exposes `run_191(cfg)` and `reset_191()`. Config key `subsystem_191.enabled` defaults to false.\n## module_192: subsystem_192\nOwns `Subsystem192` and depends on module_191. Exposes `run_192(cfg)` and `reset_192()`. Config key `subsystem_192.enabled` defaults to false.\n## module_193: subsystem_193\nOwns `Subsystem193` and depends on module_192. Exposes `run_193(cfg)` and `reset_193()`. Config key `subsystem_193.enabled` defaults to false.\n## module_194: subsystem_194\nOwns `Subsystem194` and depends on module_193. Exposes `run_194(cfg)` and `reset_194()`. Config key `subsystem_194.enabled` defaults to false.\n## module_195: subsystem_195\nOwns `Subsystem195` and depends on module_194. Exposes `run_195(cfg)` and `reset_195()`. Config key `subsystem_195.enabled` defaults to false.\n## module_196: subsystem_196\nOwns `Subsystem196` and depends on module_195. Exposes `run_196(cfg)` and `reset_196()`. Config key `subsystem_196.enabled` defaults to false.\n## module_197: subsystem_197\nOwns `Subsystem197` and depends on module_196. Exposes `run_197(cfg)` and `reset_197()`. Config key `subsystem_197.enabled` defaults to false.\n## module_198: subsystem_198\nOwns `Subsystem198` and depends on module_197. Exposes `run_198(cfg)` and `reset_198()`. Config key `subsystem_198.enabled` defaults to false.\n## module_199: subsystem_199\nOwns `Subsystem199` and depends on module_198. Exposes `run_199(cfg)` and `reset_199()`. Config key `subsystem_199.enabled` defaults to false.\n## module_200: subsystem_200\nOwns `Subsystem200` and depends on module_199. Exposes `run_200(cfg)` and `reset_200()`. Config key `subsystem_200.enabled` defaults to false.\n## module_201: subsystem_201\nOwns `Subsystem201` and depends on module_200. Exposes `run_201(cfg)` and `reset_201()`. Config key `subsystem_201.enabled` defaults to false.\n## module_202: subsystem_202\nOwns `Subsystem202` and depends on module_201. Exposes `run_202(cfg)` and `reset_202()`. Config key `subsystem_202.enabled` defaults to false.\n## module_203: subsystem_203\nOwns `Subsystem203` and depends on module_202. Exposes `run_203(cfg)` and `reset_203()`. Config key `subsystem_203.enabled` defaults to false.\n## module_204: subsystem_204\nOwns `Subsystem204` and depends on module_203. Exposes `run_204(cfg)` and `reset_204()`. Config key `subsystem_204.enabled` defaults to false.\n## module_205: subsystem_205\nOwns `Subsystem205` and depends on module_204. Exposes `run_205(cfg)` and `reset_205()`. Config key `subsystem_205.enabled` defaults to false.\n## module_206: subsystem_206\nOwns `Subsystem206` and depends on module_205. Exposes `run_206(cfg)` and `reset_206()`. Config key `subsystem_206.enabled` defaults to false.\n## module_207: subsystem_207\nOwns `Subsystem207` and depends on module_206. Exposes `run_207(cfg)` and `reset_207()`. Config key `subsystem_207.enabled` defaults to false.\n## module_208: subsystem_208\nOwns `Subsystem208` and depends on module_207. Exposes `run_208(cfg)` and `reset_208()`. Config key `subsystem_208.enabled` defaults to false.\n## module_209: subsystem_209\nOwns `Subsystem209` and depends on module_208. Exposes `run_209(cfg)` and `reset_209()`. Config key `subsystem_209.enabled` defaults to false.\n## module_210: subsystem_210\nOwns `Subsystem210` and depends on module_209. Exposes `run_210(cfg)` and `reset_210()`. Config key `subsystem_210.enabled` defaults to false.\n## module_211: subsystem_211\nOwns `Subsystem211` and depends on module_210. Exposes `run_211(cfg)` and `reset_211()`. Config key `subsystem_211.enabled` defaults to false.\n## module_212: subsystem_212\nOwns `Subsystem212` and depends on module_211. Exposes `run_212(cfg)` and `reset_212()`. Config key `subsystem_212.enabled` defaults to false.\n## module_213: subsystem_213\nOwns `Subsystem213` and depends on module_212. Exposes `run_213(cfg)` and `reset_213()`. Config key `subsystem_213.enabled` defaults to false.\n## module_214: subsystem_214\nOwns `Subsystem214` and depends on module_213. Exposes `run_214(cfg)` and `reset_214()`. Config key `subsystem_214.enabled` defaults to false.\n## module_215: subsystem_215\nOwns `Subsystem215` and depends on module_214. Exposes `run_215(cfg)` and `reset_215()`. Config key `subsystem_215.enabled` defaults to false.\n## module_216: subsystem_216\nOwns `Subsystem216` and depends on module_215. Exposes `run_216(cfg)` and `reset_216()`. Config key `subsystem_216.enabled` defaults to false.\n## module_217: subsystem_217\nOwns `Subsystem217` and depends on module_216. Exposes `run_217(cfg)` and `reset_217()`. Config key `subsystem_217.enabled` defaults to false.\n## module_218: subsystem_218\nOwns `Subsystem218` and depends on module_217. Exposes `run_218(cfg)` and `reset_218()`. Config key `subsystem_218.enabled` defaults to false.\n## module_219: subsystem_219\nOwns `Subsystem219` and depends on module_218. Exposes `run_219(cfg)` and `reset_219()`. Config key `subsystem_219.enabled` defaults to false.\n\n# Question\nIn the repository map above, how often is the auth signing key rotated, and which function performs the rotation? Answer in one sentence, citing the module.\n\n# Answer\n"
}